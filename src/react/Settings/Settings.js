import React from "react"
import "./settings.css"

export default class Settings extends React.Component {
   render() {
      return (
         <div id="settingsSection">

         </div>
      );
   }
}